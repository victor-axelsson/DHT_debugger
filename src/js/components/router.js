import React from 'react'
import { CONSTANTS } from 'core/constants'

//import Home from './home'


export function getRoute(route) {
    // console.log(route);
    /*
    if (route.name == CONSTANTS.HOME) {
        return (<Home {...route.passProps}/>);
    } else if (route.name == CONSTANTS.SHOP) {
        return (<Shop {...route.passProps}/>);
    } else if (route.name == CONSTANTS.WISH) {
        return (<Wish {...route.passProps}/>);
    } else if (route.name == CONSTANTS.OFFERS) {
        return (<Offers {...route.passProps}/>);
    } else if (route.name == CONSTANTS.ACCOUNT) {
        return (<Account {...route.passProps}/>);
    } else if (route.name == CONSTANTS.BASKET) {
        return (<Basket {...route.passProps}/>);
    } else {

        //Default route
        return (<Home {...route.passProps}/>);
    }
    */
}